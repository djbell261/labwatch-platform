spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-service-test-group
    listener:
      auto-startup: false
  mail:
    host: ""
    port: 587
    username: ""
    password: ""

logging:
  level:
    root: WARN

app:
  kafka:
    topic:
      alert-events: alert-events
      ai-investigation-events: ai-investigation-events

labwatch:
  notifications:
    cooldown-seconds: 300
    email:
      enabled: false
      to: test@example.com
      from: labwatch@localhost
